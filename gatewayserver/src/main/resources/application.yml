server:
  port: 8080

spring:
  application:
    name: gatewayserver

  config:
    import: optional:configserver:http://localhost:8071

  cloud:
    config:
      fail-fast: true

eureka:
  instance:
    prefer-ip-address: true